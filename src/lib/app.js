const $=s=>document.querySelector(s);const $$=s=>document.querySelectorAll(s);
function copyText(text){navigator.clipboard?.writeText(text).then(()=>toast("تم النسخ")).catch(()=>toast("تعذر النسخ"))}
function toast(msg){let t=$("#toast");if(!t){t=document.createElement("div");t.id="toast";t.style="position:fixed;bottom:20px;left:20px;z-index:100;background:#0f172a;color:#fff;padding:12px 16px;border-radius:12px";document.body.append(t)}t.textContent=msg;setTimeout(()=>t.remove(),1800)}
function generateCCcam(){const hosts=["srv1.streamx-free.net","sat99.fastcline.org","express.cccam-node.io"];const host=hosts[Math.floor(Math.random()*hosts.length)],port=[19850,12000,18900][Math.floor(Math.random()*3)],user="user_"+Math.random().toString(36).slice(2,8),pass="pass_"+Math.random().toString(36).slice(2,9);const line=`C: ${host} ${port} ${user} ${pass}`;$("#cccam-result").classList.remove("hidden");$("#res-host").textContent=host;$("#res-port").textContent=port;$("#res-user").textContent=user;$("#res-pass").textContent=pass;$("#res-code").textContent=line;$("#copy-config").onclick=()=>copyText(line)}
function filterPlaylists(){const q=($("#iptv-search")?.value||"").toLowerCase();$$(".playlist").forEach(x=>x.classList.toggle("hidden",q&&!x.textContent.toLowerCase().includes(q)))}
document.addEventListener("DOMContentLoaded",()=>{const toggle=$("#dark-mode-toggle");toggle?.addEventListener("click",()=>{document.documentElement.dataset.theme=document.documentElement.dataset.theme==="light"?"dark":"light"});$("#generate-cccam")?.addEventListener("click",generateCCcam);$("#iptv-search")?.addEventListener("input",filterPlaylists);const consent=$("#cookie-consent");if(consent&&!localStorage.getItem("streamx-consent"))consent.classList.remove("hidden");$("#cookie-accept")?.addEventListener("click",()=>{localStorage.setItem("streamx-consent","1");consent.classList.add("hidden")});});